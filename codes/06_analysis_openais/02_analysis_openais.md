```prompt
현재 파일(2-1 naverapi_shop4.py)을 openpyxl 로 동작하게 수정 -동일 폴더에 알맞은 파일명으로 작성
- genai_rpa.xlsx은 동일 폴더에 작업(os.path.dirname(os.path.abspath(file)))
- api_key는 환경변수로 설정
```

```prompt
add next cell with  naver news  in 'now_report' sheet
- 같은 폴더에 02_analysis_with_news_openais.py로 저장
```

```python